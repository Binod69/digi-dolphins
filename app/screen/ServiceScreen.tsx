import { CounterService, Outstanding, ServiceBanner } from '../Components';

const Service = () => {
  return (
    <>
      <ServiceBanner />
      <Outstanding />
      <CounterService />
    </>
  );
};

export default Service;
